namespace Commons.Utils
{
    /// <summary>
    /// Defines supported script types for batch processing.
    /// </summary>
    public enum ScriptType
    {
        /// <summary>Python script (.py).</summary>
        Python,
        /// <summary>Grasshopper script (.gh).</summary>
        Grasshopper,
        /// <summary>Grasshopper XML script (.ghx).</summary>
        GrasshopperXml
    }
}